#include <stdio.h>

int main() {
    char a;
    scanf("%c",&a);
    if(A<=c && c<=Z){
        printf("Uppercase");
    }
    else if(a<=c && c<=z){
        printf("Lowercase");
    }
    else{
        printf("Not an alphabet");
    }
    return 0;
}